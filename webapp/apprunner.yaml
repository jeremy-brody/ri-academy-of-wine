version: 1.0
runtime: nodejs22
build:
  commands:
    pre-build:
      - npm ci
    build:
      - npm run build
run:
  command: node build
  network:
    port: 3000
    env: PORT
  env:
    - name: PORT
      value: "3000"
